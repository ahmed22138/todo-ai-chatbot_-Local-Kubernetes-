# PostgreSQL Database values
image:
  repository: postgres
  tag: "16-alpine"
  pullPolicy: IfNotPresent

nameOverride: ""
fullnameOverride: ""

# PostgreSQL configuration
postgresql:
  database: todo_chatbot
  username: todouser
  password: todopass123  # Change in production
  port: 5432

# Persistence
persistence:
  enabled: true
  size: 5Gi
  storageClass: standard

# Resources (reduced for low memory)
resources:
  limits:
    cpu: 300m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi

# Service
service:
  type: ClusterIP
  port: 5432
